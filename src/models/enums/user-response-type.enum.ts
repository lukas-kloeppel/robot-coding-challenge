export enum UserResponseType {
  MESSAGE,  // message is a regular communication message
  INFO,     // info is a system message for example if there was a wrong input
  ERROR     // error is shown if an application error happened
}
